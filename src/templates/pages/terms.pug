extends ../settings/layout-policy

prepend html
  - const lang = locale;

prepend head
  - const pageTitle = "Terms of Service";
  - const themeColor = "#ffffff";

append head

append content
  include ../components/termsPage
